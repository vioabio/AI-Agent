<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出：本地开发用文本格式 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - traceId=%mdc{traceId} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出：生产用 JSON 格式 -->
    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/vio-ai-agent.jsonl</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/vio-ai-agent.%d{yyyy-MM-dd}.jsonl</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.JsonEncoder">
            <!-- 自动输出所有 MDC 字段（traceId/spanId/sessionId/agentType 等） -->
        </encoder>
    </appender>

    <!-- Spring Profile 控制 -->
    <springProfile name="local,test">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
        <logger name="com.vio.vioaiagent" level="DEBUG" />
        <logger name="org.springframework.ai" level="DEBUG" />
    </springProfile>

    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="JSON_FILE" />
        </root>
        <logger name="com.vio.vioaiagent" level="INFO" />
    </springProfile>
</configuration>

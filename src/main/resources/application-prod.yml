# ============================================================
# 生产环境配置（--spring.profiles.active=prod）
# 覆盖 application.yml 中的默认配置
# 注意：不要提交 API Key 等敏感信息到此文件
# ============================================================
spring:
  autoconfigure:
    exclude:
      # 生产环境无 PostgreSQL，排除 PGVector 自动配置
      - org.springframework.ai.autoconfigure.vectorstore.pgvector.PgVectorStoreAutoConfiguration
      # 排除 DataSource 自动配置（无数据库）
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration
      - org.springframework.boot.autoconfigure.jdbc.JdbcTemplateAutoConfiguration
  ai:
    dashscope:
      # 从环境变量 AI_DASHSCOPE_API_KEY 读取，不要硬编码
      api-key: ${AI_DASHSCOPE_API_KEY}
    # 生产环境关闭 MCP（简化部署，需要时开启并配置 MCP Server 地址）
    mcp:
      client:
        enabled: false

# 生产环境日志级别（减少日志输出，节约存储）
logging:
  level:
    org.springframework.ai: INFO
    com.vio.vioaiagent: INFO
